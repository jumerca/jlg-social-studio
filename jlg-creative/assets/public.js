const API='https://wxurfggrvyggqexvjpqi.supabase.co/functions/v1/jlg-api';
const money=v=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(v||0));
const root=document.querySelector('#packsRoot'),featuredRoot=document.querySelector('#featuredRoot'),modal=document.querySelector('#modalRoot'),toast=document.querySelector('#toast');
const extras=[['print','Support imprimé supplémentaire',90],['social','Lot de 5 visuels réseaux supplémentaires',120],['page','Page web supplémentaire',180],['video','Vidéo promo courte et simple',190]];
let packs=[];
const featured=[
{id:'featured-essential',name:'Pack Essentiel',category:'Essentiel',description:'Pour lancer une communication claire et professionnelle sans multiplier les supports inutiles.',price:490,delay:'5 à 7 jours ouvrés',deliverables:['Mini direction visuelle','1 support principal au choix','5 visuels réseaux sociaux','Fichiers prêts à l’emploi']},
{id:'featured-identity',name:'Pack Identité & Visuels',category:'Image de marque',description:'Pour construire une image cohérente et disposer de visuels que vous pourrez réellement réutiliser.',price:890,delay:'7 à 10 jours ouvrés',deliverables:['Direction graphique','Identité visuelle légère','2 supports de communication','Kit réseaux sociaux']},
{id:'featured-digital',name:'Site Vitrine Clé en Main',category:'Digital',description:'Pour présenter votre activité avec un site simple et professionnel, livré prêt à utiliser sans maintenance mensuelle imposée.',price:1490,delay:'12 à 18 jours ouvrés',deliverables:['Site vitrine jusqu’à 5 pages','Version mobile','Formulaire de contact','SEO local de base','Mise en ligne initiale','Guide de prise en main']}
];
const findPack=id=>[...featured,...packs].find(x=>x.id===id);
function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
function showToast(t,bad=false){toast.textContent=t;toast.className=bad?'show bad':'show';setTimeout(()=>toast.className='',2800);}
async function fetchJSON(url,opts={}){const ctrl=new AbortController();const timer=setTimeout(()=>ctrl.abort(),15000);try{const r=await fetch(url,{...opts,signal:ctrl.signal});const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'Erreur');return j}finally{clearTimeout(timer)}}
async function load(){renderFeatured();try{const j=await fetchJSON(API+'?action=catalog');packs=(j.packs||[]).filter(p=>p.active!==false);renderPacks();}catch(e){root.innerHTML='<div class="empty emptyLarge">Les offres métiers ne peuvent pas être chargées pour le moment.</div>';}}
function featuredCard(p){return `<article class="featuredCard"><div class="featuredTop"><span class="featuredIcon">${p.id==='featured-essential'?'◉':p.id==='featured-identity'?'✦':'▣'}</span><span class="pill">${esc(p.category)}</span></div><h3>${esc(p.name)}</h3><p>${esc(p.description)}</p><div class="featuredDelay"><small>Délai indicatif</small><strong>${esc(p.delay)}</strong></div><div class="featuredPrice"><small>À partir de</small><strong>${money(p.price)}</strong></div><div class="featuredActions"><button class="secondary" data-detail="${p.id}">Détails</button><button class="primary" data-order="${p.id}">Demander</button></div></article>`;}
function renderFeatured(){featuredRoot.innerHTML=featured.map(featuredCard).join('');}
function renderPacks(){if(!packs.length){root.innerHTML='<div class="empty emptyLarge">Les offres métiers sont en cours de préparation.</div>';return}root.innerHTML=packs.map(p=>`<article class="sectorCard"><div><span class="pill">${esc(p.category||'Pack')}</span><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p></div><div class="sectorMeta"><span><small>Délai</small><strong>${esc(p.delay||'À définir')}</strong></span><span><small>À partir de</small><strong>${money(p.price)}</strong></span></div><div class="sectorActions"><button class="secondary" data-detail="${p.id}">Voir le détail</button><button class="primary" data-order="${p.id}">Demander</button></div></article>`).join('');}
function mountDialog(html){modal.innerHTML=`<div class="overlay"><section class="modal" role="dialog" aria-modal="true">${html}</section></div>`;const box=modal.querySelector('.modal');setTimeout(()=>box?.querySelector('button,input,textarea,select')?.focus(),20);const close=()=>{modal.innerHTML=''};modal.querySelector('.overlay').addEventListener('mousedown',e=>{if(e.target.classList.contains('overlay'))close()});const escClose=e=>{if(e.key==='Escape'){close();document.removeEventListener('keydown',escClose)}};document.addEventListener('keydown',escClose);return {box,close};}
function detail(p){const {box,close}=mountDialog(`<button class="close" aria-label="Fermer">×</button><div class="modalHero"><span class="pill">${esc(p.category||'Pack')}</span><h2>${esc(p.name)}</h2><p>${esc(p.description||'')}</p></div><div class="meta"><div><small>Délai indicatif</small><strong>${esc(p.delay||'À définir')}</strong></div><div><small>Budget indicatif</small><strong>${p.price?money(p.price):'Sur devis'}</strong></div></div><div class="deliver"><h3>Ce que comprend le pack</h3>${(p.deliverables||[]).map(x=>`<div><span aria-hidden="true">✓</span><span>${esc(x)}</span></div>`).join('')||'<p>Contenu défini selon votre projet.</p>'}</div><div class="info">L’envoi d’une demande ne vaut pas commande ferme : JLG Creative vous adresse d’abord un devis. Après livraison, vous restez autonome ; toute évolution future fait l’objet d’un nouveau devis.</div><button class="primary full" data-order="${p.id}">Demander ce pack →</button>`);box.querySelector('.close').onclick=close;box.querySelector('[data-order]').onclick=()=>{close();order(p)}}
function validEmail(v){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v.trim())}
function order(pack){let step=1,opts=[];const state={name:'',company:'',email:'',phone:'',city:'',objective:'',deadline:'',budget:'',style:'',notes:'',website:'',contactPreference:'Email'};let dialog=null;function capture(){dialog?.box.querySelectorAll('input,textarea,select').forEach(el=>{if(el.name)state[el.name]=el.value})}function draw(){const estimate=Number(pack.price||0)+opts.reduce((s,k)=>s+(extras.find(x=>x[0]===k)?.[2]||0),0);let body='';if(step===1)body=`<h3>Vos coordonnées</h3><p>Pour préparer le devis et vous recontacter.</p><div class="formGrid"><label>Nom / prénom *<input name="name" required autocomplete="name" value="${esc(state.name)}"></label><label>Structure<input name="company" autocomplete="organization" value="${esc(state.company)}"></label><label>E-mail *<input name="email" required type="email" autocomplete="email" value="${esc(state.email)}"></label><label>Téléphone<input name="phone" autocomplete="tel" value="${esc(state.phone)}"></label><label>Ville<input name="city" autocomplete="address-level2" value="${esc(state.city)}"></label><label class="hp" aria-hidden="true">Site web<input name="website" tabindex="-1" autocomplete="off"></label><label>Contact préféré<select name="contactPreference"><option>Email</option><option ${state.contactPreference==='Téléphone'?'selected':''}>Téléphone</option></select></label></div>`;if(step===2)body=`<h3>Votre besoin</h3><p>Plus le brief est clair, plus la proposition sera juste.</p><div class="formGrid"><label class="wide">Objectif du projet *<textarea name="objective" required rows="4" placeholder="Ex. lancer mon activité, préparer un événement, refaire ma communication…">${esc(state.objective)}</textarea></label><label>Date souhaitée<input name="deadline" type="date" value="${esc(state.deadline)}"></label><label>Budget envisagé<input name="budget" value="${esc(state.budget)}" placeholder="Ex. 800 à 1 200 €"></label><label class="wide">Style / ambiance<input name="style" value="${esc(state.style)}" placeholder="Moderne, chaleureux, premium, sportif…"></label><label class="wide">Précisions<textarea name="notes" rows="4">${esc(state.notes)}</textarea></label></div>`;if(step===3)body=`<h3>Options utiles</h3><p>Facultatives. Elles servent uniquement à affiner votre demande.</p><div class="options">${extras.map(([k,l,p])=>`<button type="button" class="option ${opts.includes(k)?'active':''}" data-opt="${k}"><b>${opts.includes(k)?'✓':'+'}</b><span><strong>${l}</strong><small>+ ${money(p)}</small></span></button>`).join('')}</div>`;if(step===4)body=`<h3>Récapitulatif</h3><div class="summary"><div><small>Offre</small><strong>${esc(pack.name)}</strong></div><div><small>Demandeur</small><strong>${esc(state.name)}${state.company?' · '+esc(state.company):''}</strong></div><div><small>Objectif</small><strong>${esc(state.objective)}</strong></div><div><small>Estimation indicative</small><strong>${estimate?money(estimate):'Sur devis'}</strong></div><div><small>Options</small><strong>${opts.length?opts.map(k=>esc(extras.find(x=>x[0]===k)?.[1])).join(', '):'Aucune'}</strong></div></div><div class="info successInfo">Aucun paiement n’est demandé. Cette demande donnera lieu à un devis avant engagement.</div><p class="privacyNote">En envoyant votre demande, vous autorisez JLG Creative à utiliser ces informations pour vous répondre et préparer votre projet. <a href="./legal.html" target="_blank" rel="noopener">Confidentialité</a>.</p>`;if(dialog)dialog.close();dialog=mountDialog(`<button class="close" aria-label="Fermer">×</button><div class="orderHead"><div><span class="pill">${esc(pack.category||'Projet')}</span><h2>${esc(pack.name)}</h2></div><div class="dots" aria-label="Étape ${step} sur 4">${[1,2,3,4].map(n=>`<b class="${step>=n?'on':''}">${n}</b>`).join('')}</div></div><div class="orderBody">${body}</div><div class="orderNav">${step>1?'<button class="secondary" id="back">← Retour</button>':'<span></span>'}${step<4?'<button class="primary" id="next">Continuer →</button>':'<button class="primary" id="send">Envoyer ma demande</button>'}</div>`);dialog.box.classList.add('orderModal');dialog.box.querySelector('.close').onclick=dialog.close;dialog.box.querySelectorAll('input,textarea,select').forEach(el=>el.addEventListener('input',()=>state[el.name]=el.value));dialog.box.querySelectorAll('[data-opt]').forEach(btn=>btn.onclick=()=>{capture();const k=btn.dataset.opt;opts=opts.includes(k)?opts.filter(x=>x!==k):[...opts,k];draw()});dialog.box.querySelector('#back')?.addEventListener('click',()=>{capture();step--;draw()});dialog.box.querySelector('#next')?.addEventListener('click',()=>{capture();if(step===1&&(!state.name.trim()||!validEmail(state.email)))return showToast('Nom et e-mail valide sont obligatoires.',true);if(step===2&&!state.objective.trim())return showToast('Décrivez l’objectif du projet.',true);step++;draw()});dialog.box.querySelector('#send')?.addEventListener('click',async e=>{capture();const send=e.currentTarget;send.disabled=true;send.textContent='Envoi…';const payload={packId:String(pack.id||'').startsWith('featured-')?'':(pack.id||''),packName:pack.name,packPrice:Number(pack.price||0),estimate,options:opts.map(k=>{const x=extras.find(e=>e[0]===k);return {key:x[0],label:x[1],price:x[2]}}),...state};try{const j=await fetchJSON(API+'?action=order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});dialog.close();dialog=mountDialog(`<div class="successModal"><div class="check">✓</div><h2>Demande envoyée</h2><p>Votre demande a bien été transmise.</p><div class="ref"><small>Référence</small><strong>${esc(j.reference)}</strong></div><p class="muted">Vous recevrez un devis avant toute validation définitive.</p><button class="primary closeFinal">Terminer</button></div>`);dialog.box.querySelector('.closeFinal').onclick=dialog.close}catch(e){showToast(e.name==='AbortError'?'Le serveur met trop de temps à répondre.':e.message||'Erreur lors de l’envoi',true);send.disabled=false;send.textContent='Envoyer ma demande'}})}draw()}
document.addEventListener('click',e=>{const detailBtn=e.target.closest('[data-detail]'),orderBtn=e.target.closest('[data-order]'),custom=e.target.closest('[data-custom]');if(detailBtn){const p=findPack(detailBtn.dataset.detail);if(p)detail(p)}if(orderBtn){const p=findPack(orderBtn.dataset.order);if(p)order(p)}if(custom)order({id:'',name:'Projet sur mesure',category:'Sur mesure',description:'',price:0,delay:'À définir',deliverables:[]})});
const menuBtn=document.querySelector('#menuBtn'),nav=document.querySelector('#mainNav');menuBtn.onclick=()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.textContent=open?'×':'☰'};nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');menuBtn.textContent='☰'}));
const shareSite=document.querySelector('#shareSite');if(shareSite)shareSite.onclick=async()=>{const payload={title:'JLG Creative',text:'Découvrez JLG Creative : communication, design, digital et demandes de projets en ligne.',url:new URL('./index.html',location.href).href};try{if(navigator.share)await navigator.share(payload);else{await navigator.clipboard.writeText(payload.url);showToast('Lien JLG Creative copié.')}}catch(e){if(e?.name!=='AbortError')showToast('Impossible de partager pour le moment.',true)}};
load();
